pub fn annotate(minefield: &[&str]) -> Vec<String> {
//32 is empty 42 is mine
        for field in minefield.iter().enumerate() {
            for symbol in field.1.as_bytes(){

            }
        }
    
    Vec::new()
}
